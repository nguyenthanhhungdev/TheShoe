import { Controller } from '@nestjs/common';

@Controller('cart')
export class CartController {
  constructor(/* private readonly cartService: CartService */) {}

  // Define endpoints: get cart, add item, update item, remove item, clear cart
}

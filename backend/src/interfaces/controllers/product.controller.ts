import { Controller } from '@nestjs/common';

@Controller('products')
export class ProductController {
  constructor(/* private readonly productService: ProductService */) {}

  // Define endpoints: create, update, delete, get by id, list
}

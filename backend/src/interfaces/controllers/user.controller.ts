import { Controller } from '@nestjs/common';

@Controller('users')
export class UserController {
  constructor(/* private readonly userService: UserService */) {}

  // Define endpoints: register, login, profile, update, delete
}
